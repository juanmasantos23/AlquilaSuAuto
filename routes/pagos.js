const { Router } = require('express');

const router = Router();

// Aquí se agregarán las rutas de pago más adelante
// Ejemplo:
// router.post('/procesar', validarJWT, procesarPago);

module.exports = router;
